package br.com.dlo.run;

public class ExecutionPart1 {

	public static void main(String[] args) {

		/*
		 * EntityManagerFactory entityManagerFactory =
		 * Persistence.createEntityManagerFactory("part1-DIO"); EntityManager
		 * entityManager = entityManagerFactory.createEntityManager();
		 * 
		 * 
		 * 
		 * Estado estado = new Estado("São Paulo", "SP"); Aluno aluno = new
		 * Aluno("Daniel", 29, estado);
		 * 
		 * 
		 * entityManager.getTransaction().begin(); entityManager.persist(estado);
		 * entityManager.persist(aluno); entityManager.getTransaction().commit();
		 * 
		 * entityManager.close(); entityManagerFactory.close();
		 */

	}

}
